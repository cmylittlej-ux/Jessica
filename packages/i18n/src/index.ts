/**
 * @reos/i18n
 *
 * Bilingual by design (Spec §2.7, §25). Three language dimensions are kept
 * strictly separate and the original is never overwritten:
 *
 *   Original Language   — as received, immutable
 *   Working Language    — user's reading/drafting language (Chinese | English)
 *   Sending Language    — what the recipient actually receives
 */
export const PACKAGE_NAME = '@reos/i18n';
