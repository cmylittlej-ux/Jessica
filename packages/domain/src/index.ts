/**
 * @reos/domain
 *
 * Core domain model and services (Spec §2.1 Domain First, §2.2 Case First).
 * Phase 1 delivers: property / contact / case / communication / task /
 * approval / activity modules with repository + service layers.
 */
export const PACKAGE_NAME = '@reos/domain';
