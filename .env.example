# Copy to .env.local and adjust. Never commit real secrets (Spec §30).

# PostgreSQL via docker compose (pnpm db:up)
DATABASE_URL=postgresql://reos:reos@localhost:5432/reos

# AI provider abstraction (Spec §2.4 / §8) — mock first, no real keys in Phase 0
AI_PROVIDER=mock
# AI_API_KEY=          # server-only, never in source code

# Connector toggles — Foundation MVP is mock-only (Spec §0)
EMAIL_CONNECTOR=mock
PROPERTY_CONNECTOR=mock
CRM_CONNECTOR=mock
