# WorkBuddy environment compatibility (see docs/decisions/ADR-0002):
# the filesystem broker denies pnpm's default symlink-heavy virtual-store
# layout inside node_modules/.pnpm. Hoisted linker + copy imports produce
# an npm-like flat node_modules without those symlinks.
node-linker=hoisted
package-import-method=copy
